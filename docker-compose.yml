services:
  torch:
    build:
      context: .
      dockerfile: Dockerfile
    image: howird/vid2avatar
    runtime: nvidia
    volumes:
      - .:/vid2avatar
    ipc: host
    tty: True
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
